spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
      use-new-id-generator-mappings: true
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    username: root
    password:
    url: jdbc:h2:/data/jobms
    driver-class-name: org.h2.Driver
